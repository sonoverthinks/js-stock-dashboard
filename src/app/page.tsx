import { Button } from "@/components/ui/button";
const Page = () => {
  return (
    <div className="p-4">
      <Button className="hover:cursor-pointer">Click me</Button>
    </div>
  );
};

export default Page;
